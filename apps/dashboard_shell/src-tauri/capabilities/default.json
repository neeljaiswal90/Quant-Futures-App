{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the MNQ dashboard shell.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}

