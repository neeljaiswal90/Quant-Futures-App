# QFA-612-BROKER-01 SDK migration substrate.
# async-rithmic is the sole Rithmic SDK for the broker session sidecar.
# Tests mock the import boundary so CI does not require gateway access or the package.
async-rithmic==1.6.1
