"""Rithmic Analytics — MNQ futures analytics layer.

Public surface is exposed via submodules:
    rithmic_analytics.core      — loaders, schemas, contract metadata
    rithmic_analytics.features  — VP, CVD, footprint, absorption, ATR
    rithmic_analytics.ops       — rotation, alerts, rollover calendar
    rithmic_analytics.viewer    — HTML report rendering
    rithmic_analytics.cli       — one-shot command-line entry points
"""

__version__ = "0.1.0"
